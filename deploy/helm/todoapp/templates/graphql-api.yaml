apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "todoapp.fullname" . }}-graphql-api
  labels:
    {{- include "todoapp.labels" . | nindent 4 }}
    app.kubernetes.io/component: graphql-api
spec:
  replicas: {{ .Values.replicas.graphqlApi }}
  selector:
    matchLabels:
      {{- include "todoapp.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: graphql-api
  template:
    metadata:
      labels:
        {{- include "todoapp.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: graphql-api
      annotations:
        checksum/env-common: {{ toYaml .Values.env.common | sha256sum | quote }}
        {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      containers:
        - name: graphql-api
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command:
            - "/graphql-api"
          envFrom:
            - configMapRef:
                name: {{ include "todoapp.commonEnvConfigMapName" . }}
          env:
            - name: OTEL_SERVICE_NAME
              value: "graphql-api"
            - name: GRAPHQL_SERVER_PORT
              value: "8085"
            - name: {{ .Values.env.secrets.keys.llmApiKey }}
              valueFrom:
                secretKeyRef:
                  name: {{ include "todoapp.secretName" . }}
                  key: {{ .Values.env.secrets.keys.llmApiKey }}
                  optional: {{ .Values.env.secrets.optional }}
            - name: {{ .Values.env.secrets.keys.llmEmbeddingApiKey }}
              valueFrom:
                secretKeyRef:
                  name: {{ include "todoapp.secretName" . }}
                  key: {{ .Values.env.secrets.keys.llmEmbeddingApiKey }}
                  optional: {{ .Values.env.secrets.optional }}
            - name: {{ .Values.env.secrets.keys.mcpGatewayApiKey }}
              valueFrom:
                secretKeyRef:
                  name: {{ include "todoapp.secretName" . }}
                  key: {{ .Values.env.secrets.keys.mcpGatewayApiKey }}
                  optional: {{ .Values.env.secrets.optional }}
          ports:
            - containerPort: 8085
              name: http
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 20
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 6
          resources:
            {{- toYaml .Values.resources.app | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "todoapp.fullname" . }}-graphql-api
  labels:
    {{- include "todoapp.labels" . | nindent 4 }}
    app.kubernetes.io/component: graphql-api
spec:
  type: {{ .Values.services.graphql.type }}
  selector:
    {{- include "todoapp.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: graphql-api
  ports:
    - name: http
      port: {{ .Values.services.graphql.port }}
      targetPort: http
      protocol: TCP
      {{- if or (eq .Values.services.graphql.type "NodePort") (eq .Values.services.graphql.type "LoadBalancer") }}
      nodePort: {{ .Values.services.graphql.nodePort }}
      {{- end }}
